generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider  = "postgresql"
  url       = env("DATABASE_URL")
  directUrl = env("DIRECT_URL")
}

model User {
  id           String         @id @default(uuid())
  username     String
  password     String
  email        String         @unique
  createdAt    DateTime       @default(now())
  lastUpdate   DateTime       @updatedAt
  avatarUrl    String?

  watchProgress WatchProgress[] @relation("Users progress")
}

model Video {
  id        String      @id @default(uuid())
  name      String
  description String?
  length    Int?
  thumbnail String
  banner    String
  rls_year  Int?
  genre_id  Int
  videotype VideoType
  favorites Favorites[]
  genre     Genre       @relation(fields: [genre_id], references: [id])
  seasons   Season[]    @relation("VideoToSeason")
  createdAt DateTime @default(now())

  watchProgress WatchProgress[] @relation("Video Progression")
}

model Season { 
  id String @id @default(uuid())
  name String
  number Int

  videoId String
  video Video @relation(fields: [videoId], references: [id], name: "VideoToSeason")

  episodes Episode[] @relation("Episodes Season ")

  createdAt DateTime @default(now())
  lastUpdate DateTime @updatedAt @default(now())
}

model Episode {
  id            String   @id @default(uuid())
  title         String
  description   String?
  number        Int
  
  length        Int      
  thumbnail     String?
  
  season_id     String
  season        Season   @relation(fields: [season_id], references: [id], onDelete: Cascade, name: "Episodes Season ")
  
  createdAt     DateTime @default(now())

  watchProgress WatchProgress[] @relation("Episode Progression")
}

model WatchProgress {
  id            String   @id @default(uuid())
  user_id       String
  video_id      String?
  episode_id    String?
  type          VideoType
  last_position BigInt
  isFinished    Boolean

  user          User @relation(fields: [user_id], references: [id], name: "Users progress")
  video         Video? @relation(fields: [video_id], references: [id], name: "Video Progression")
  episode       Episode? @relation(fields: [episode_id], references: [id], name: "Episode Progression")
}

model Genre {
  id    Int  @id @default(autoincrement())
  name  String
  video Video[]
}

model Favorites {
  id       String   @id @default(uuid())
  video_id String
  addedAt  DateTime
  video    Video    @relation(fields: [video_id], references: [id])
}

enum VideoType {
  MOVIE
  SERIES 
}
